server:
  port: 8089

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tasksPlannerTest
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
